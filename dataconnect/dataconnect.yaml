specVersion: "v1beta"
serviceId: "gmailclone"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "gmailclone-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
